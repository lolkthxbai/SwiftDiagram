# Contributing

Before contributing:

- Open an issue before proposing a major behavioral or architectural change.
- Keep syntax, semantic model, validation, rendering, configuration, SwiftSyntax inspection, and CLI concerns separated.
- Run `swift build` and `swift test` before submitting changes.
